import com.sun.istack.internal.logging.Logger;
import com.sun.javafx.util.Logging;

import sun.rmi.runtime.Log;
import sun.util.logging.resources.logging;

public class RealClass {
 public static void main(String[] args) {
    logging.clearCache();
    Logger.getLogger(componentClass);
    Logging.getInputLogger().getName();
    //java.util.logging.Logger.global.wait();
    
    String test = "testing string";
    //TODO - implement class instances

    test("Testing name");

    return;
 }

 public static boolean test(String name) {
     //TODO: Do some work here related to testing the name
     return false;
 }
}